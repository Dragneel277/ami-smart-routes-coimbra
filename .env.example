DB_USER=postgres
DB_PASSWORD=change_me
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smart_routes

# Safer default for tests. Use roads_raw only when intentionally rebuilding.
DB_TABLE=roads_raw_test
DB_IF_EXISTS=replace
